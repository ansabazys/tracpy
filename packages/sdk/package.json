{
  "name": "@repo/sdk",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsc --watch",
    "build:browser": "esbuild src/browser.ts --bundle --minify --format=iife --outfile=dist/script.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}